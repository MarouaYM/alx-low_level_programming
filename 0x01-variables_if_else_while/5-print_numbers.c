#include <stdio.h>
/**
*main - entry point
*Description : " I never thought that it would pay off"
*Return: always 0 (success)
*/
int main(void)
{
int n;
for (n = 0; n <= 9; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
